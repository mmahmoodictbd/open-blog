databaseChangeLog:

  - changeSet:
      id: create-sample-site-additional-props
      author: mahmood
      changes:
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: metaKeywords
              - column:
                  name: property_value
                  value: Hello World, Ideas
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialLinkedInUrl
              - column:
                  name: property_value
                  value: http://www.linkedin.com/pub/mossaddeque-mahmood/39/50/702
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialGithubUrl
              - column:
                  name: property_value
                  value: https://github.com/mmahmoodictbd
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialFBUrl
              - column:
                  name: property_value
                  value: https://www.facebook.com/mmahmood.ict.bd
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialTwitterUrl
              - column:
                  name: property_value
                  value: https://twitter.com/mmahmood_ict_bd
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialGooglePlusUrl
              - column:
                  name: property_value
                  value: https://plus.google.com/115803378661601558710
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialWikiPageUrl
              - column:
                  name: property_value
                  value: http://mmahmoodict.wikidot.com/
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: socialEmailMe
              - column:
                  name: property_value
                  value: mahmood@unloadbrain.com
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: googleAnalyticsAccountId
              - column:
                  name: property_value
                  value: UA-121247298-1
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: googleSiteVerificationId
              - column:
                  name: property_value
                  value: M7l9TnjwuSL-wpXX8bQwMx9WPY71lj-_YU0mw7XoBHc
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: bingSiteVerificationId
              - column:
                  name: property_value
                  value: 86F638A63C724C5E67117AB3B8A092B0
      - insert:
          tableName: site_additional_properties
          columns:
              - column:
                  name: site_id
                  # Braces around the SQL is necessary, liquibase could not generate proper MySQL SQL.
                  # MySQL expects braces around SELECT query in INSERT VALUES.
                  valueComputed: "(SELECT id FROM site LIMIT 1)"
              - column:
                  name: property_key
                  value: disqusUniqueUrl
              - column:
                  name: property_value
                  value: unloadbrain.disqus.com